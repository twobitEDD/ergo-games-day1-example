# Core API
DAY1_API_PORT=4010
# Railway: set DAY1_API_PORT=$PORT in service env vars.
DAY1_DB_PATH=./.day1-data/day1.sqlite
# Comma-separated browser origins allowed to call cookie-auth endpoints.
DAY1_CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Session cookie policy.
# Local default is Strict; production default is None+Secure for split-domain deployments.
# Override only when your deploy topology requires it.
# DAY1_SESSION_COOKIE_SAME_SITE=strict
# DAY1_SESSION_COOKIE_SECURE=false
# DAY1_SESSION_COOKIE_DOMAIN=

# Auth/security baseline
DAY1_TOTP_ENCRYPTION_KEY=replace-with-strong-random-value

# Dynamic-first auth bridge (recommended)
# Minimal required server vars for Dynamic JWT verification:
DAY1_DYNAMIC_AUTH_ENABLED=true
DAY1_DYNAMIC_JWT_ISSUER=
DAY1_DYNAMIC_JWT_AUDIENCE=
DAY1_DYNAMIC_JWKS_URL=

# Ergo submit/status defaults for hosted test deployments
DAY1_CHAIN_NETWORK=testnet
DAY1_ERGO_NODE_URL=https://api-testnet.ergoplatform.com
DAY1_ERGO_EXPLORER_URL=https://api-testnet.ergoplatform.com

# VRF adapter (Day1 randomness test)
# Use DAY1_VRF_ADAPTER_MODE=mock for deterministic local-only tests.
# Use DAY1_VRF_ORACLE_URL to call the Railway VRF oracle coordinator + operator services.
DAY1_VRF_ADAPTER_MODE=http_oracle
DAY1_VRF_ORACLE_URL=
DAY1_VRF_MAX_SUBMISSIONS=2

# Frontend Dynamic-first gate
VITE_DAY1_DYNAMIC_ENABLED=true
# Optional: set false to defer Dynamic SDK loading until user action.
VITE_DAY1_DYNAMIC_AUTOSTART=true
# Dynamic publishable environment UUID from dashboard (required when enabled).
VITE_DYNAMIC_ENVIRONMENT_ID=

# Guest fallback
# No extra env vars required. If Dynamic is unavailable/misconfigured, Day1 UI
# can bootstrap a guest session via POST /api/auth/guest.
